#!/bin/bash -e

cat <<EOF > "${DEBIRF_ROOT}/root/update"
#!/bin/sh -e

mount /boot/
cd /boot/
wget \$1/initrd -O initrd.new
mv initrd.new initrd
cd /
umount /boot/
sync
sync
sync
EOF

chmod a+x "${DEBIRF_ROOT}/root/update"
